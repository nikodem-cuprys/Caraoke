# Core scientific/audio stack (no heavy model downloads required)
numpy>=1.26,<2.0
scipy>=1.11
librosa>=0.10.2
soundfile>=0.12.1
requests>=2.32
python-dotenv>=1.0

# Automatic speech recognition with word-level timestamps.
# MIT licensed, CTranslate2-based reimplementation of Whisper, maintained by
# SYSTRAN. See README.md "Models Used" for the license/maintenance summary.
faster-whisper>=1.0.3

# Source separation. MIT licensed (facebookresearch/demucs, now maintained
# at github.com/adefossez/demucs). Pulls in torch/torchaudio; install the
# CPU wheel explicitly on machines without a CUDA GPU, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install demucs
demucs>=4.0.1

# Test-only / dev tooling
pytest>=8.3
ruff>=0.6
